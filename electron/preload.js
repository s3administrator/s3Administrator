// No bridge currently needed: the renderer talks to the local Next.js server
// over fetch. Keep the file so the BrowserWindow webPreferences.preload path
// resolves; future native bridges (file pickers, OS notifications) go here.
