services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      args:
        NEXT_PUBLIC_GA_MEASUREMENT_ID: ${NEXT_PUBLIC_GA_MEASUREMENT_ID}
        ENABLE_ACTION_TRACKING: ${ENABLE_ACTION_TRACKING:-true}
    ports:
      - "3000:3000"
    environment:
      - ENVIRONMENT=${ENVIRONMENT:-DEV}
      - DATABASE_URL=${DATABASE_URL_DOCKER:-postgresql://s3admin:${POSTGRES_PASSWORD}@db:5432/s3_admin}
      - AUTH_SECRET=${AUTH_SECRET}
      - AUTH_URL=${AUTH_URL:-http://localhost}
      - NEXT_PUBLIC_GA_MEASUREMENT_ID=${NEXT_PUBLIC_GA_MEASUREMENT_ID}
      - ENABLE_ACTION_TRACKING=${ENABLE_ACTION_TRACKING:-true}
      # OAuth — pass both _DEV and _PROD; the app resolves based on ENVIRONMENT
      - GITHUB_CLIENT_ID_DEV=${GITHUB_CLIENT_ID_DEV:-}
      - GITHUB_CLIENT_SECRET_DEV=${GITHUB_CLIENT_SECRET_DEV:-}
      - GITHUB_CLIENT_ID_PROD=${GITHUB_CLIENT_ID_PROD:-}
      - GITHUB_CLIENT_SECRET_PROD=${GITHUB_CLIENT_SECRET_PROD:-}
      - GOOGLE_CLIENT_ID_DEV=${GOOGLE_CLIENT_ID_DEV:-}
      - GOOGLE_CLIENT_SECRET_DEV=${GOOGLE_CLIENT_SECRET_DEV:-}
      - GOOGLE_CLIENT_ID_PROD=${GOOGLE_CLIENT_ID_PROD:-}
      - GOOGLE_CLIENT_SECRET_PROD=${GOOGLE_CLIENT_SECRET_PROD:-}
      - RESEND_API_KEY=${RESEND_API_KEY}
      - EMAIL_FROM=${EMAIL_FROM}
      - ENCRYPTION_MASTER_KEY=${ENCRYPTION_MASTER_KEY}
      - ENCRYPTION_SALT=${ENCRYPTION_SALT}
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
      - STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}
      - ADMIN_EMAILS=${ADMIN_EMAILS}
      - SYSTEM_LOG_FILE_PATH=${SYSTEM_LOG_FILE_PATH:-/app/logs/system.log}
      - SERVER_METRICS_ENABLED=${SERVER_METRICS_ENABLED:-true}
      - SERVER_METRICS_INTERVAL_SECONDS=${SERVER_METRICS_INTERVAL_SECONDS:-60}
      - SERVER_METRICS_RETENTION_DAYS=${SERVER_METRICS_RETENTION_DAYS:-7}
      - SERVER_METRICS_NODE_EXPORTER_URL=${SERVER_METRICS_NODE_EXPORTER_URL:-http://node-exporter:9100/metrics}
      - THUMBNAIL_S3_ENDPOINT=${THUMBNAIL_S3_ENDPOINT}
      - THUMBNAIL_S3_REGION=${THUMBNAIL_S3_REGION}
      - THUMBNAIL_S3_ACCESS_KEY=${THUMBNAIL_S3_ACCESS_KEY}
      - THUMBNAIL_S3_SECRET_KEY=${THUMBNAIL_S3_SECRET_KEY}
      - THUMBNAIL_S3_BUCKET=${THUMBNAIL_S3_BUCKET}
      - THUMBNAIL_MAX_WIDTH=${THUMBNAIL_MAX_WIDTH:-480}
      - THUMBNAIL_URL_TTL_SECONDS=${THUMBNAIL_URL_TTL_SECONDS:-3600}
    volumes:
      - app_logs:/app/logs
    depends_on:
      db:
        condition: service_healthy
      node-exporter:
        condition: service_started
    restart: unless-stopped

  tools:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      target: tools
    environment:
      - ENVIRONMENT=${ENVIRONMENT:-DEV}
      - DATABASE_URL=${DATABASE_URL_DOCKER:-postgresql://s3admin:${POSTGRES_PASSWORD}@db:5432/s3_admin}
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
      - STRIPE_SECRET_KEY_DEV=${STRIPE_SECRET_KEY_DEV:-}
      - STRIPE_SECRET_KEY_PROD=${STRIPE_SECRET_KEY_PROD:-}
    depends_on:
      db:
        condition: service_healthy

  proxy:
    image: caddy:2.10-alpine
    ports:
      - "80:80"
      - "443:443"
    environment:
      - DOMAIN=${DOMAIN:-www.s3administrator.com}
      - ROOT_DOMAIN=${ROOT_DOMAIN:-s3administrator.com}
    volumes:
      - ./${CADDYFILE:-Caddyfile.local}:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      app:
        condition: service_started
    restart: unless-stopped

  node-exporter:
    image: prom/node-exporter:v1.9.1
    command:
      - --path.rootfs=/host
      - --path.procfs=/host/proc
      - --path.sysfs=/host/sys
      - --collector.filesystem.mount-points-exclude=^/(dev|proc|sys|var/lib/docker/.+|var/lib/kubelet/.+)($|/)
    volumes:
      - /:/host:ro
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    ports:
      - "${POSTGRES_PORT:-5433}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=s3admin
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=s3_admin
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U s3admin -d s3_admin"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  pgdata:
  caddy_data:
  caddy_config:
  app_logs:
