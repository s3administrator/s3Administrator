services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      args:
        ENVIRONMENT: ${ENVIRONMENT:-COMMUNITY}
        NEXT_PUBLIC_GA_MEASUREMENT_ID: ${NEXT_PUBLIC_GA_MEASUREMENT_ID}
        ENABLE_ACTION_TRACKING: ${ENABLE_ACTION_TRACKING:-true}
        NPM_TOKEN: ${NPM_TOKEN:-}
        NEXT_PUBLIC_EDITION: ${NEXT_PUBLIC_EDITION:-community}
    ports:
      - "3000:3000"
    environment:
      - ENVIRONMENT=${ENVIRONMENT:-COMMUNITY}
      - DATABASE_URL=${DATABASE_URL_DOCKER:-postgresql://s3admin:${POSTGRES_PASSWORD}@db:5432/s3_admin}
      - AUTH_SECRET=${AUTH_SECRET}
      - AUTH_URL=${AUTH_URL:-http://localhost}
      - NEXT_PUBLIC_GA_MEASUREMENT_ID=${NEXT_PUBLIC_GA_MEASUREMENT_ID}
      - ENABLE_ACTION_TRACKING=${ENABLE_ACTION_TRACKING:-true}
      - NEXT_PUBLIC_EDITION=${NEXT_PUBLIC_EDITION:-community}
      # OAuth — env-aware: ENVIRONMENT selects _COMMUNITY or _CLOUD variant
      - GITHUB_CLIENT_ID_COMMUNITY=${GITHUB_CLIENT_ID_COMMUNITY:-}
      - GITHUB_CLIENT_SECRET_COMMUNITY=${GITHUB_CLIENT_SECRET_COMMUNITY:-}
      - GITHUB_CLIENT_ID_CLOUD=${GITHUB_CLIENT_ID_CLOUD:-}
      - GITHUB_CLIENT_SECRET_CLOUD=${GITHUB_CLIENT_SECRET_CLOUD:-}
      - GOOGLE_CLIENT_ID_COMMUNITY=${GOOGLE_CLIENT_ID_COMMUNITY:-}
      - GOOGLE_CLIENT_SECRET_COMMUNITY=${GOOGLE_CLIENT_SECRET_COMMUNITY:-}
      - GOOGLE_CLIENT_ID_CLOUD=${GOOGLE_CLIENT_ID_CLOUD:-}
      - GOOGLE_CLIENT_SECRET_CLOUD=${GOOGLE_CLIENT_SECRET_CLOUD:-}
      - RESEND_API_KEY=${RESEND_API_KEY}
      - EMAIL_FROM=${EMAIL_FROM}
      - ENCRYPTION_MASTER_KEY=${ENCRYPTION_MASTER_KEY}
      - ENCRYPTION_SALT=${ENCRYPTION_SALT}
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
      - STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}
      - ADMIN_EMAILS=${ADMIN_EMAILS}
      - SYSTEM_LOG_FILE_PATH=${SYSTEM_LOG_FILE_PATH:-/app/logs/system.log}
      - SERVER_METRICS_ENABLED=${SERVER_METRICS_ENABLED:-true}
      - SERVER_METRICS_INTERVAL_SECONDS=${SERVER_METRICS_INTERVAL_SECONDS:-60}
      - SERVER_METRICS_RETENTION_DAYS=${SERVER_METRICS_RETENTION_DAYS:-7}
      - SERVER_METRICS_NODE_EXPORTER_URL=${SERVER_METRICS_NODE_EXPORTER_URL:-http://node-exporter:9100/metrics}
      - THUMBNAIL_S3_ENDPOINT=${THUMBNAIL_S3_ENDPOINT}
      - THUMBNAIL_S3_REGION=${THUMBNAIL_S3_REGION}
      - THUMBNAIL_S3_ACCESS_KEY=${THUMBNAIL_S3_ACCESS_KEY}
      - THUMBNAIL_S3_SECRET_KEY=${THUMBNAIL_S3_SECRET_KEY}
      - THUMBNAIL_S3_BUCKET=${THUMBNAIL_S3_BUCKET}
      - THUMBNAIL_MAX_WIDTH=${THUMBNAIL_MAX_WIDTH:-480}
      - THUMBNAIL_URL_TTL_SECONDS=${THUMBNAIL_URL_TTL_SECONDS:-3600}
    volumes:
      - app_logs:/app/logs
    depends_on:
      db:
        condition: service_healthy
      node-exporter:
        condition: service_started
    restart: unless-stopped

  tools:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      target: tools
      args:
        NPM_TOKEN: ${NPM_TOKEN:-}
    environment:
      - ENVIRONMENT=${ENVIRONMENT:-COMMUNITY}
      - DATABASE_URL=${DATABASE_URL_DOCKER:-postgresql://s3admin:${POSTGRES_PASSWORD}@db:5432/s3_admin}
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
      - STRIPE_SECRET_KEY_COMMUNITY=${STRIPE_SECRET_KEY_COMMUNITY:-}
      - STRIPE_SECRET_KEY_CLOUD=${STRIPE_SECRET_KEY_CLOUD:-}
    depends_on:
      db:
        condition: service_healthy

  proxy:
    image: caddy:2.10-alpine
    ports:
      - "80:80"
      - "443:443"
    environment:
      - DOMAIN=${DOMAIN:-localhost}
      - ROOT_DOMAIN=${ROOT_DOMAIN:-localhost}
      - CADDY_SITE_ADDRESSES=${CADDY_SITE_ADDRESSES:-:80}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      app:
        condition: service_started
    restart: unless-stopped

  node-exporter:
    image: prom/node-exporter:v1.9.1
    command:
      - --path.rootfs=/host
      - --path.procfs=/host/proc
      - --path.sysfs=/host/sys
      - --collector.filesystem.mount-points-exclude=^/(dev|proc|sys|var/lib/docker/.+|var/lib/kubelet/.+)($|/)
    volumes:
      - /:/host:ro
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    ports:
      - "${POSTGRES_PORT:-5433}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=s3admin
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=s3_admin
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U s3admin -d s3_admin"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  pgdata:
  caddy_data:
  caddy_config:
  app_logs:
